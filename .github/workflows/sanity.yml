name: sanity 

on:
  workflow_call:
    inputs:
      ip_address:
        type: string

jobs:
  example_job:
    name: Generate output
    runs-on: ubuntu-latest
    steps:
      - name: use the ip
        run: echo "${{ inputs.ip_address }}"
